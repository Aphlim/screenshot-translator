node_modules/
out/
dist/
.vite/
*.log
.build-log.txt
.DS_Store

# Electron build artifacts
release/
release-build/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code local-only settings (per-machine permissions, hooks)
.claude/settings.local.json

# Local config / secrets (electron-store writes to %APPDATA%, but in case
# anyone copies a config file into the project, never commit it)
config.json
.env
.env.local
